{% form_theme form 'gamereqs/form_row.html.twig' %}


{{ form_start(form) }}
    {{ form_widget(form, { 'attr' : { 'class': 'class' } }) }}

    
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
